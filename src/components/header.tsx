import Image from "next/image";
export default function Header() {
    return (
      <header className="fixed z-50 w-full bg-cyan-600 flex items-center justify-between text-white">
        <h2 className="place-center m-2">Dashboard</h2>  

      </header>
    );
  }
  